<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 网络权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- 前台 Service 权限（音乐播放）-->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <!-- Android 13+ 通知权限 (前台服务播放控制及通知栏展示) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 应用内安装 APK 权限 -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:name=".MoodyMusicApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MoodyMusicForAndroid"
        tools:targetApi="31">
        <activity
            android:name=".ui.home.activity.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MoodyMusicForAndroid">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.test.activity.ApiTestActivity"
            android:exported="true"
            android:label="API 测试"
            android:theme="@style/Theme.MoodyMusicForAndroid" />

        <activity
            android:name=".ui.auth.activity.LoginActivity"
            android:exported="true"
            android:label="登录"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.MoodyMusicForAndroid"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ui.classroom.activity.ClassroomActivity"
            android:exported="true"
            android:label="教室"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.MoodyMusicForAndroid" />

        <!-- 极光推送 Receiver -->
        <receiver
            android:name=".receiver.JPushReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="cn.jpush.android.intent.RECEIVE_MESSAGE" />
                <category android:name="${applicationId}" />
            </intent-filter>
        </receiver>

        <!-- 极光推送 Service -->
        <service
            android:name=".receiver.PushService"
            android:process=":pushcore"
            android:exported="false">
            <intent-filter>
                <action android:name="cn.jiguang.user.service.action" />
            </intent-filter>
        </service>

        <!-- 音乐播放前台 Service -->
        <service
            android:name=".ui.player.MusicPlayerService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <!-- FileProvider 配置 (用于在线更新 APK 安装) -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>